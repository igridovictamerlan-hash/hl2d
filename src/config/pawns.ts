import type { FactionId } from './factions';

/**
 * Персонажи в стиле пешек RimWorld: туловище-«фасолина» цвета формы (цвет ранга), крупная голова,
 * толстый тёмный контур. Четыре стороны: юг — лицо, север — затылок, восток/запад — профиль.
 * Размеры — px мира от центра персонажа (радиус столкновений 12; пешка выше круга).
 */
export const PAWN = {
  outline: '#151515',
  outlineWidth: 1.5,
  /** Туловище: верх (плечи) и низ, полуширина у плеч и в поясе. */
  body: { top: -5, bottom: 14, shoulder: 7.5, waist: 8.8 },
  /** Голова: центр и радиус; в профиле — сдвиг вперёд. */
  head: { y: -10, r: 8, sideShift: 1.4 },
  eye: { dx: 3, y: -9, r: 1.15, color: '#141414' },
  /** Тень под ногами (эллипс). */
  shadow: { y: 13, rx: 9.5, ry: 3.5, color: 'rgba(0,0,0,0.32)' },
  /** Кожа и волосы — по персонажу (от его id / имени). */
  skins: ['#f1c9a5', '#e2b48c', '#d3a077', '#bf8a60', '#9c6b45', '#7a4f33'],
  hairs: ['#2b2320', '#4a3322', '#6b4a2e', '#8a6a44', '#b8905a', '#3a3a3a', '#9a9a9a', '#6e2f1e'],
  /** Причёски: доля лысых и длинных (остальные — короткие). */
  baldChance: 0.12,
  longChance: 0.25,
  /** Лежащий (тело): доля яркости формы и поворот. */
  corpseAlpha: 0.85,
  /** Выделение игрока — полукольцо у ног. */
  playerRing: 'rgba(255,211,107,0.9)',
  /** Снаряжение по фракциям (что на голове и детали формы). */
  gear: {
    citizen: { head: 'hair' },
    cwu: { head: 'cap', cap: '#d6b24a' },
    rebel: { head: 'bandana', cloth: '#6b7a3a' },
    cp: { head: 'mask', helmet: '#2c3138', mask: '#c9ccd1', lens: '#1b1f24', lensRim: '#8fb8d8' },
    ota: { head: 'ota', helmet: '#3a4250', visor: '#ff4a3a' },
    admin: { head: 'hair', collar: '#f4f4f4', tie: '#7a1c1c' },
  } as Record<FactionId, Record<string, string>>,
  /** С какого ранга у повстанцев бандана (младшие — просто с волосами). */
  rebelBandanaRank: 1,
} as const;
