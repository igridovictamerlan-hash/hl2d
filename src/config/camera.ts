export const CAMERA = {
  /** Сколько пикселей мира видно на экране (минимум по каждой оси). */
  viewWidth: 600,
  viewHeight: 400,
  /** Скорость догоняния цели (1/с), экспоненциальное сглаживание. */
  followRate: 9,
  /** Сдвиг камеры в сторону курсора: доля расстояния и максимум в px. */
  lookAheadFactor: 0.18,
  lookAheadMax: 44,
} as const;
