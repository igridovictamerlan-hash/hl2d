/** Война на границе: отряды повстанцев у пограничных КПП, прорывы, красный код. */
export const WAR = {
  /** Первый отряд на каждом КПП появится через… (перестрелка начинается почти сразу). */
  firstSquad: [2, 6] as const,
  squadSize: [3, 4] as const,
  /**
   * Ритм фронта: отряды по squadSize подходят раз в squadGap с и собираются на пустоши вне
   * видимости постов (режим gather), пока их не станет capture.minAttackers — тогда все разом
   * идут на капт. Больше maxRebels на фронте не бывает. Пока КПП у повстанцев, подходят
   * пополнения до minRebels.
   */
  minRebels: 3,
  maxRebels: 8,
  squadGap: [4, 9] as const,
  /** Точка сбора: от внешних ворот не ближе/не дальше, px; не видна с постов часовых. */
  gatherDist: [170, 360] as const,
  /**
   * Состав отряда: у всех автоматы — трофейный MP7, у части трофейный AR2 (rifleChance);
   * дробовик (у штурмовых — вдвое чаще), снайпер-арбалетчик и командир (AR2 + револьвер).
   */
  rifleChance: 0.3,
  shotgunChance: 0.2,
  marksmanChance: 0.35,
  commanderChance: 0.2,
  /** Штурмовой отряд (forceAssault, оголённый пост) перестреливается столько, потом идёт в коридор. */
  assaultAfter: [20, 45] as const,
  /** КПП без часовых на постах столько секунд — отряд идёт на прорыв. */
  pushWhenUnguarded: 45,
  /** Отряд меняет позицию раз в… */
  relocateEvery: [12, 25] as const,
  /** Позиция «за укрытием»: завал на линии огня не дальше, px. */
  coverReach: 48,
  /** Огонь на подавление по постам, когда часовых не видно: пауза между очередями и длина очереди. */
  suppressEvery: [2.5, 6] as const,
  suppressBurst: [2, 4] as const,

  /** Сколько бойцов ГО держать на каждом КПП (часовые GRID на постах + медик HELIX). */
  guardsPerFront: 3,
  medicPerFront: 1,
  /** Подкрепление через… после потери; прибывает к внутренним воротам КПП. Во время капта — не прибывает. */
  reinforceDelay: 10,

  /** Красный код: сколько отрядов OTA, минимум длительности, отбой через… после уничтожения прорвавшихся. */
  otaSquad: 4,
  /** Доля OTA с дробовиками. */
  otaShotgunChance: 0.25,
  redMinTime: 45,
  calmToGreen: 15,
  redMaxTime: 180,
  /** Сколько даётся гражданам, чтобы уйти с улиц после объявления комендантского часа. */
  curfewGrace: 25,
  /** Укрытия граждан: не ближе этого к занятым другими (px), иначе — штраф к «расстоянию». */
  shelterSpacing: 40,
  shelterCrowdPenalty: 400,
  /** Раз в сколько секунд «Надзор» засекает прорвавшихся (последняя известная позиция). */
  overwatchScan: 6,
  /**
   * «Капт» КПП (как захват территории в SAMP): на точке сбора собралось ≥ minAttackers повстанцев —
   * все разом идут на захват, на duration с. Пока идёт капт, подкрепления ГО на этот КПП не
   * приходят (и новые отряды повстанцев тоже) — бой тех, кто есть. КПП захвачен, если перебит
   * гарнизон, стоявший на КПП в начале капта, или у повстанцев killsToWin убийств с перевесом;
   * по таймеру — захвачен при ≥ minKills и перевесе. Все штурмующие погибли или ушли — капт отбит.
   * Следующий сбор — через cooldown. Захваченный КПП: красный код, повстанцы на постах;
   * контрудары Альянса через holdTime, КПП отбит, когда в его зоне нет повстанцев retakeCalm с.
   */
  capture: {
    minAttackers: 5,
    duration: 120,
    killsToWin: 5,
    minKills: 3,
    cooldown: 25,
    firstAfter: 10,
    holdTime: 60,
    retakeCalm: 10,
    /** Контрудар по захваченному КПП: отряд GRID из стольких бойцов раз в retakeEvery с. */
    retakeSquad: 4,
    retakeEvery: 35,
    /**
     * Продвижение в капте перебежками: цель — укрытие в коридоре на доле пути advance (0 — внешние
     * ворота, 1 — посты); дошёл — держится coverWait с и продвигается на advanceStep. Под огнём —
     * стреляет shootStop с, потом бежит dash с, не залипая на месте.
     */
    advanceStep: 0.25,
    coverWait: [1, 2.5] as const,
    shootStop: [1, 1.6] as const,
    dash: [1.2, 2] as const,
  },
  /** Бой у КПП «идёт», если там стреляли за последние… секунд (маркеры на экране, звук). */
  activeWindow: 3,
} as const;
