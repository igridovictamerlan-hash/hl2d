/** Бой. Урон — единицы здоровья, время — секунды, расстояния — px. */
export const COMBAT = {
  /** Бронежилеты: множитель входящего урона по фракциям. */
  armor: { cp: 0.8, ota: 0.6 } as Record<string, number>,
  /** Шанс, что пуля остановится о бетонный блок-укрытие. */
  barrierStopChance: 0.45,
  /** Свой блок рядом со стрелком не мешает стрелять поверх. */
  ownCoverDistance: 26,
  /** Разброс в движении умножается. */
  movingSpreadMul: 1.8,
  /** Раненый (доля здоровья) отходит. */
  woundedFraction: 0.35,
  /** Сколько лежит тело. */
  corpseTime: 90,
  /** Игрок возрождается через… */
  respawnDelay: 6,
  /** Регенерация, если давно не ранили. */
  regenDelay: 12,
  regenPerSec: 0.5,
  /** Регенерация только до этой доли здоровья. */
  regenCap: 0.6,
  /** Сколько видны трассеры и вспышки. */
  tracerTime: 0.09,
  impactTime: 0.35,
  /** Слышимость выстрела: NPC в радиусе реагируют. */
  hearing: 360,
  /** Медик HELIX лечит за раз и перезаряжается. */
  healAmount: 25,
  healCooldown: 2.5,
  healRange: 36,
  /** ИИ: задержка реакции, очередь, пауза между очередями. */
  ai: { reaction: [0.25, 0.6] as const, burst: [2, 5] as const, burstPause: [0.5, 1.4] as const, aimError: 1.3 },
} as const;
