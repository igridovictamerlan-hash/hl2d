/**
 * Спецотряды ГО (по мотивам сервера). Время — секунды, расстояния — px.
 *  OBS (id jury) — сканирует тела и находит убийцу: тот объявляется в розыск, «Надзор» знает, где он;
 *  TECH (id tech) — запускает сканер: дрон летает над кварталами и засекает повстанцев и нарушителей.
 */
export const CP_UNITS = {
  obs: {
    /** Сканирование тела, дальность, радиус поиска тел NPC-наблюдателем. */
    scanTime: 3,
    reach: 40,
    seek: 360,
  },
  scanner: {
    /** Сколько летает, перерыв между запусками, скорость, высота «зависания» (визуально). */
    life: 100,
    cooldown: 25,
    speed: 70,
    /** Облетает точки в этом радиусе от техника. */
    roam: 460,
    /** Засекает в этом радиусе (сверху видно — стены не мешают, но уровень тот же). */
    sight: 150,
    scanEvery: 0.6,
    /** Одного и того же — не чаще. */
    reportEvery: 12,
    /** Вспышка «фото». */
    flash: 0.25,
  },
} as const;
