export const GAME = {
  /** Частота логики (фиксированный шаг). */
  tickRate: 60,
  /** Максимум шагов логики за кадр (защита от «спирали смерти»). */
  maxStepsPerFrame: 5,
  /** Интервал обновления DOM-HUD, с. */
  hudInterval: 0.1,
  /** Не показывать название одной и той же зоны чаще, с. */
  zoneBannerCooldown: 12,
  /** Сколько нужно пробыть в зоне, чтобы показать название, с. */
  zoneBannerDelay: 0.35,
} as const;
