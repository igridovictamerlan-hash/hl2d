/** Размеры мира. Радиус персонажа — в entities.ts. */
export const WORLD = {
  /** Размер тайла в пикселях мира. */
  tileSize: 16,
  /** Размер карты в тайлах: 188 × 16 = 3008 px. */
  widthTiles: 188,
  heightTiles: 188,
  /**
   * Канализация — отдельная область той же сетки справа от города (зазор gap тайлов):
   * город в масштабе scale (люк в западном квартале ведёт на запад канализации).
   * Итоговая ширина карты: 188 + gap + 188 × scale.
   */
  underground: { gap: 4, scale: 0.5 },
} as const;
