/**
 * Кражи (вор, отброс общества). Время — секунды, расстояния — px, деньги — токены.
 * Карманная кража — встать за спиной у прохожего и выждать time; взлом раздатчика рационов —
 * вору с отмычкой, пока окно закрыто. ГО, увидевший вора в течение seenFor с после кражи, —
 * нарушение «кража» (арест). Жертва замечает с шансом noticeChance и кричит — ГО в cryRange идёт на крик.
 */
export const CRIME = {
  pickpocket: {
    time: 1.4,
    reach: 32,
    /** Вор должен быть за спиной: угол между взглядом жертвы и направлением на вора больше, °. */
    behindDeg: 110,
    amount: [4, 12] as const,
    /** Вору — добыча больше. */
    thiefMul: 1.6,
    noticeChance: 0.3,
    cryRange: 320,
  },
  hack: { time: 6, rations: 3, lockpickBreak: 0.5 },
  /**
   * Гоп-стоп (бандит): лицом к лицу в подворотне (жилые кварталы, промзона), ствол в руках, выдержка
   * time с — жертва отдаёт amount токенов и кричит с шансом cryChance. NPC-бандит выходит на дело
   * с шансом npcChance, ищет жертву в seek px, когда ГО не видно.
   */
  rob: { time: 1.4, reach: 46, amount: [10, 24] as const, cryChance: 0.75, npcChance: 0.6, seek: 220, zones: ['residential', 'industrial'] as const },
  seenFor: 5,
  /** NPC-вор: шанс пойти на дело, где искать жертву, у жертвы должно быть не меньше minMoney. */
  npc: { chance: 0.5, seek: 260, minMoney: 6, giveUp: 18 },
} as const;
