/** Видимость, туман войны и двери. Расстояния — px мира. */
export const VISION = {
  /** Лучей на круг при построении области видимости игрока. */
  rays: 900,
  /** Дальность обзора (больше половины диагонали экрана 600×400). */
  radius: 470,
  /** Насколько луч заходит в стену — чтобы видимая грань стены была освещена. */
  wallBleed: 6,
  /** Цвет затемнения невидимого. */
  fogColor: 'rgba(3,5,8,0.74)',
  /** Доля радиуса, с которой видимость плавно гаснет к краю. */
  fadeStart: 0.78,

  /** Зрение NPC: дальность, угол обзора (градусы), «чуйка» вплотную (видят и за спиной). */
  npcRange: 210,
  npcFovDeg: 140,
  npcCloseAwareness: 44,
} as const;

export const DOORS = {
  /** Дверь открывается, если кто-то ближе этого к её центру (плюс половина размера двери). */
  openRadius: 24,
  /** Закрывается через столько секунд после того, как рядом никого. */
  closeDelay: 1.2,
  /** Как часто проверять двери, с. */
  interval: 0.1,
} as const;
