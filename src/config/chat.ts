/** Чат и команды игрока. Расстояния — px, время — с. */
export const CHAT = {
  maxLength: 140,
  /** Сколько висит реплика над головой. */
  bubbleTime: 4,
  /** Слышимость речи (реакции NPC). */
  hearRange: 220,
  greetRange: 90,
  /** Поощрение: гражданин не дальше. */
  rewardRange: 60,
  /** Слова, на которые ГО реагирует как на оскорбление, и приветствия. */
  insult: /(свин|мусор|урод|сволоч|пс[иы]н|коз[её]л|шестерк|шестёрк|комбайн[а-я]* под|ублюд)/i,
  greeting: /(привет|здравствуй|здорово|добрый (день|вечер)|хай\b)/i,
} as const;
