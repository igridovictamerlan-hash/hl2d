/**
 * Канализация, люки, повстанцы в городе (саботаж, засады), тревога Администратора, чёрный рынок.
 * Время — секунды, расстояния — px.
 */
export const UNDERGROUND = {
  /** Люк: на каком расстоянии можно пользоваться, сколько секунд лезть. */
  useRadius: 30,
  climbTime: 1.2,
} as const;

export const INSURGENCY = {
  /** Партизан в схроне — ROSTER.partisans (постоянный состав, config/roster.ts). */
  /** Операции из убежища: первая через…, потом раз в… */
  firstOp: [20, 40] as const,
  opEvery: [45, 90] as const,
  /**
   * Вылазки поодиночке и парами, пока нет операции и в убежище остаётся ≥ minAtBase бойцов:
   * раз в outingEvery с — обход тоннелей до случайного люка, поход на рынок или разведка в город
   * через люк (outingKinds — доли); на месте — outingWait с, потом назад. Разведчик в городе
   * отходит не дальше scoutRadius тайлов от люка.
   */
  outingEvery: [6, 12] as const,
  minAtBase: 1,
  outingKinds: { tunnels: 0.5, market: 0.2, scout: 0.3 },
  /** Разведчик не стреляет первым: заметил ГО — уходит к люку; отвечает, если ранили за … с. */
  returnFireFor: 5,
  outingWait: [3, 8] as const,
  scoutRadius: [6, 16] as const,
  /** Доля саботажей (остальное — засады на ГО). */
  sabotageChance: 0.55,
  /** Размер группы: саботаж, засада. */
  sabotageTeam: [1, 2] as const,
  ambushTeam: [2, 3] as const,
  /** Сколько возиться с узлом Альянса; награда повстанцу-игроку. */
  sabotageTime: 5,
  sabotageReward: 15,
  /** Тайник убежища: патронов до стольких магазинов к каждому стволу. */
  cacheMags: 4,
  /** Засада: сколько вести бой после первого выстрела, потом отход к люку. */
  ambushFight: [18, 30] as const,
  /** Засада не ближе этого к Нексусу и КПП (нападают на патрули в кварталах). */
  ambushAvoidZones: ['nexus', 'cells', 'checkpoint', 'outlands', 'restricted', 'wasteland', 'rebel_camp'] as const,
  /** Бойцы убежища бродят в пределах… */
  baseWander: [2, 7] as const,
} as const;

/** Тревога Администратора при нападениях и саботаже в городе (код жёлтый). */
export const ALARM = {
  /** Минимальная длительность, отбой после стольких секунд без известных нападавших. */
  minTime: 40,
  calmToGreen: 20,
  /** Сколько держится точка тревоги (куда стягиваются патрули), если нападавших не видно. */
  pointTime: 45,
  /** Патрули ГО в этом радиусе от тревоги идут на помощь. */
  respondRadius: 1100,
  /** Убито сотрудников Альянса в городе за тревогу — эскалация до красного кода. */
  escalateKills: 3,
} as const;
