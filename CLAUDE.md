# CLAUDE.md — Сити-17 (HL2RP 2D)

Браузерная 2D-игра с видом сверху в духе HL2RP-серверов Garry's Mod: тесный город из переулков под
контролем Альянса. Все персонажи — пешки в стиле RimWorld (цвет формы по рангу) с подписью имени/роли. Игрок управляет одним,
остальные — NPC, ведущие себя как игроки RP-сервера.

**Язык:** интерфейс, комментарии и документация — по-русски; идентификаторы в коде — по-английски.

## Команды

```bash
npm install
npm run dev        # http://localhost:5173
npm test           # vitest: генератор, A*, коллизии, JSON, симуляция NPC
npm run typecheck  # tsc для src и tests
npm run build      # typecheck + сборка в dist/
npm run mapstats   # метрики генератора по 30 сидам (DEBUG_GEN=1 — причины отбраковки попыток)
```

Параметры адреса: `?seed=123` (карта по seed, пишется в адрес автоматически), `?map=city17`
(загрузить `public/maps/city17.json`), `?npcs=40`, `?debug=1`.

## Стек и почему без движка

TypeScript + Vite + HTML5 Canvas 2D, без игровых движков. Всё ключевое здесь нестандартное —
генератор переулков, туман войны лучами, A* по якорям 2×2 с учётом радиуса, уступание дороги;
Phaser не дал бы готового для этого, а добавил бы свой цикл, сцену и физику, с которыми пришлось бы
бороться. Логика не зависит от DOM и целиком тестируется в Node (vitest), включая безголовую
симуляцию NPC (`tests/simHarness.ts`).

## Архитектура

```
src/
  main.ts            точка входа, параметры адреса
  config/            ВСЕ настраиваемые значения (см. ниже) — никаких «магических чисел» в коде
  core/              Game (корень), GameLoop (фикс. шаг 60 Гц + интерполяция), Input (по KeyboardEvent.code),
                     PlayerController (движение, ЛКМ/ПКМ/R/Q/H/T, E, F, G, Tab, 1/2/3), Camera (~820×550 px мира), EventBus, Rng, MinHeap, math
  world/             GameMap (данные карты + состояние дверей), tiles (типы тайлов), NavGrid (якоря 2×2),
                     collision (круг–тайлы), visibility (лучи DDA, прямая видимость), FogRenderer (туман войны),
                     connectivity (flood fill + тоннели), mapStats, mapIO (JSON), MapRenderer, EffectsRenderer
                     (тела, трассеры, поломки, узлы Альянса, люки, очередь, тревоги, указатели КПП), SpatialHash
    generator/       CityGenerator (оркестратор), layout (макроплан), lattice (решётка + лабиринт),
                     stamps + templates (площадь, Нексус, пограничные КПП, кольцо запретной зоны),
                     магазин ГСР, features (дворы, арки…), wastes (пустошь вокруг города, тропа, лагерь
                     сопротивления), sewers (канализация, два люка, схрон партизан, рынок), GenGrid
  entities/          Character (кружок: игрок и NPC одинаковы; ранг, отряд ГО, профессия, закон, инвентарь, сытость, оружие),
                     Inventory, EntityManager, physics, factory, EntityRenderer (тела до тумана, подписи после),
                     PawnRenderer (пешки в стиле RimWorld), PawnCache (спрайты пешек и тел),
                     WeaponRenderer + weaponPose (оружие в руках)
  ai/                AStar, smoothing, PathService (очередь с бюджетом), Mover (движение/уступание),
                     yieldSearch (поиск «кармана»), StateMachine, Brain, facing, destinations,
                     Gunner (стрелок ИИ, угол обзора, тревога, гранаты), Dodge (бегство от гранат),
                     streetBark (реплики на улице), HatchTravel (пути через люки),
                     brains/: CitizenBrain (граждане, ГСР, подпольщики, воры, бандиты; уличная жизнь), CpBrain (ГО), RebelBrain
                     (армия сопротивления: лагерь, сбор, капт, клич), DefectorBrain (гражданин бежит к прорванному КПП), UndergroundBrain (убежище: саботаж, засады), OtaBrain, CrematorBrain, PrisonerBrain, PostBrain
  systems/           Roster (постоянный состав: роли и возрождение), RebelCommand (глава, цель, клич),
                     StreetLife (уличная жизнь: бочки с огнём, дома, скамейки и фонари проспекта, обращения Администратора),
                     ElectionSystem (выборы Администратора), LaborSystem (завод, коробки, мусор, медики,
                     крематор), CrimeSystem (кражи, гоп-стоп, взлом, скан тел),
                     ScannerSystem (дроны TECH), ZoneSystem, DoorSystem (двери), LawSystem (закон, КПЗ), EconomySystem (рационы, голод,
                     зарплаты, ремонт, узлы Альянса, магазин, чёрный рынок), CombatSystem (бой), WarSystem (граница,
                     тревоги: жёлтый/красный код), UndergroundSystem (уровни, люки), InsurgencySystem (убежище,
                     операции в городе), Loyalty (очки и уровни), ChatSystem (чат и команды), Barks (реплики в бою), SaveGame
                     (формат сохранения), Population (заселение, наборы предметов, точки появления ролей)
  ui/                DOM поверх холста: Hud, ZoneBanner, EventLog, RoleMenu, CheckPanel, InventoryPanel (Tab),
                     ShopPanel (ГСР и чёрный рынок), AlertBar, CaptureBar (капт), ChatBox (Enter), MapView
                     (мини-карта + большая карта M), DeathScreen, DevPanel (F2, генератор), DebugOverlay (F3),
                     HelpBar, GunfireAudio (звук выстрелов), styles.css
tests/               vitest; simHarness.ts — мир без DOM для тестов поведения
public/maps/         сохранённые карты (JSON), открываются через ?map=имя
```

### Тик логики (`Game.update`, 60 Гц)
1. `PlayerController`: ввод → `player.wantX/wantY`, взгляд на курсор, прицел (ПКМ), огонь, смена оружия, E/F/1/2/3.
   Задержанному игроку временно ставится `PrisonerBrain` — тогда ввод движения игнорируется.
2. `updateNpcs`: мозги (FSM) → `Mover` выставляет `wantX/wantY` → `dodgeGrenades` (граната рядом —
   бегство поверх решения мозга); затем `PathService.process()`.
3. `DoorSystem`: двери открываются, если рядом кто-то есть; закрываются через `DOORS.closeDelay`.
4. `stepPhysics`: разгон → движение → расталкивание по массам → выталкивание из стен.
5. `LawSystem.update`: неподчинение игрока, заведение в камеру, сроки, освобождение.
5a. `EconomySystem` (раздача, голод, зарплаты, поломки) → `CombatSystem` (перезарядка, регенерация,
   уборка убитых) → `WarSystem` (отряды у КПП, подкрепления, прорыв, тревоги) → `InsurgencySystem`
   (гарнизон убежища, операции) → возрождение игрока.
6. Видимость: многоугольник обзора игрока (`VISION.rays` лучей DDA) и `c.visible` у всех.
7. Камера (в границах уровня игрока; сменил уровень — `snapTo`), зоны, UI (DOM — с частотой `GAME.hudInterval`).

Отрисовка (`Game.render`): карта → терминал найма → эффекты на земле (тела, щитки, очередь) →
персонажи (только видимые; нос-треугольник — взгляд, силуэт оружия по классу) → конусы NPC →
трассеры и взмахи дубинки → туман → конус игрока → подписи и реплики → отладка → виньетка →
красный код/ранение → указатели на КПП → прицел. Интерполяция `prevX → x`.

### Ключевые понятия
- **Тайл** 16 px. Город 188×188 тайлов (3008 px) в кольце пустоши за стеной (8 тайлов слева и
  справа, 26 снизу: тропа и лагерь сопротивления) — 204×214; справа за зазором — канализация
  (масштаб 1:2), вся карта 310×214. Типы — `world/tiles.ts` (`solid`, `opaque`, символ для JSON).
  Добавляя тип, допишите его в `TILE_DEFS`, палитру `MapRenderer` и `DevPanel`.
- **Уровни** (`Level`: city / sewer) — две области одной сетки без прохода между ними; связь только
  люками. `GameMap.underground` (прямоугольник канализации по её зонам), `levelAt`, `levelBounds`;
  `NavGrid.level` по якорям. A* между уровнями сразу отказывает, `randomAnchorAround` берёт цели
  только на своём уровне, камера не выходит за границы уровня, в канализации обзор короче
  (`VISION.sewerRadius`), выстрелы с другого уровня не слышны.
- **Якорь 2×2**: персонаж радиусом 12 px занимает квадрат 2×2 тайла. Якорь (ax, ay) ↔ центр в
  ((ax+1)·16, (ay+1)·16). Связность карты, A* и поиск «карманов» работают по якорям — поэтому
  проход 2 тайла (32 px) пропускает ровно одного, 3 тайла (48 px) — двоих.
- **Зоны** — сетка `zoneGrid` (id зоны на тайл) + список `zones` (kind + имя). `ZoneKind`:
  residential, avenue, plaza, nexus, cells, industrial, restricted, checkpoint (пограничные КПП),
  outlands (пустоши за стеной), shop (магазин ГСР), sewer, rebel_base (убежище), black_market.
- **Точки интереса (POI)** — `map.pois` в координатах тайлов: лагерь сопротивления и его тайник, окно раздачи, центр площади, терминал
  найма, ворота и стойка Нексуса, терминал кодов тревоги, камеры КПЗ, ворота запретной зоны, заводские дворы, посты часовых
  КПП, выходы в пустоши, люки (`hatch` в городе / `sewer_hatch` в канализации: якорь 2×2, x,y — его
  левый верхний тайл; пары связаны одинаковым `id`), убежище, тайник, прилавок и место торговца.
- **Двери** — группы соседних тайлов `DOOR`. Состояние (закрыта/заперта) живёт в `GameMap.doorClosed/
  doorLocked`: закрытая перекрывает обзор (`isOpaque`), запертая — ещё и проход (`isSolid`).
  Запирание вызывает `NavGrid.refresh` вокруг двери.
- **Видимость**: `visibility.ts` — луч DDA до первого непрозрачного тайла. Игрок видит в радиусе
  `VISION.radius`; NPC — в `VISION.npcRange` с углом обзора `npcFovDeg` (вплотную — во все стороны).
  Одни и те же функции для игрока и NPC: от ГО можно скрыться за поворотом или за дверью.
- **Фракции и ранги** — `config/factions.ts`. Цвет формы пешки по рангу (`colorsOf`): ГО от голубого
  (RCT) до тёмно-синего (DvL), повстанцы от оранжевого (новобранец) до жёлто-золотого (командир).
  `authority` — кто проверяет, а не проверяется (ГО, OTA, администратор).

### Генератор (`world/generator/CityGenerator.ts`)
1. `layout`: магистраль по центру (+ вторая с шансом 50%), линии решётки с отступами от проспекта,
   углы запретной зоны и промзоны, площадь на длинном прямом участке проспекта, Нексус рядом.
2. `lattice`: узлы с «дрожанием» (соседи на линии смещены ≥ 3 тайла) → лабиринт «растущее дерево»
   от узлов проспекта + петли (несколько маршрутов). Рёбра вырезаются Z-образно; излом ставится так,
   чтобы прямой участок ≤ 25 тайлов и перемычка не вставала в створ соседних переулков.
3. Вырезание переулков и магистралей, чистка «шипов» (двухфазная — иначе съедаются тонкие стены).
4. Зоны, штампы: площадь с будкой и терминалом найма, Нексус по ASCII-шаблону (`templates.ts`,
   правится руками, 36×30: 7 одиночных камер КПЗ `c`, общая камера `C` → POI `common_cell` (x, y, w, h),
   коридоры в 4 тайла (Т-образный зал), двор с воротами, кабинет Администратора с
   терминалом `T`, канцелярия со столами `w` → POI `clerk_desk`, казарма ГО с нарами `b` → `bunk`,
   комната OTA `q` → `ota_spot`), кольцо запретной зоны с КПП (позиция ворот подбирается перебором),
   два пограничных КПП на концах главного проспекта (шаблон `CHECKPOINT_TEMPLATE`, восточный —
   зеркальный, 66×25): пустошь → внешние ворота → **внешний двор** (D3/D5) → два прохода, как в CS:
   **шорт** (прямой, 3 тайла) и **лонг** (обход поверху) → **внутренний двор** (D4/D6) → внутренние
   ворота → **проходная** → дверь → проспект (ось — `CHECKPOINT_AXIS_ROW`). Во дворах бетонные
   блоки-укрытия (`BARRIER`: не пройти, но видно поверх), посты `P` (2 во внешнем, 3 во внутреннем)
   и бункеры ГО. Проходная (`CHECKPOINT_GATEHOUSE_X`) отгораживает город от боя: стена-«зигзаг» на
   оси (из двора в город нет ни прямой видимости, ни прострела — даже при открытой двери), дверь со
   стороны проспекта (закрыта, пока рядом никого) и 2 поста RCT `R` (POI `gate_post`). Каждая часть —
   своя зона (`checkpointSection`, имена «… · D3», «… · шорт», «… · лонг», «… · D4», «… · проходная»).
   Пол КПП (`BUNKER`) не считается «переулком» в метрике прямых участков.
5. `features`: заводские дворы, дворы-колодцы с арками, сквозные арки (только если обход длинный;
   подъезды-коридоры с дверями отключены — `passages.archChance = 1`), тупики. Каждая деталь
   откатывается (`GenGrid.snapshot/restore`), если открывает прямой обзор длиннее нормы.
5б. **Городок** (`world/houses.ts`, `generator/homes.ts`, `HOUSES`): сплошная застройка (WALL) между
   переулками режется на участки-дома (`computeLots`: связные массивы стен рекурсивно делятся на
   прямоугольники `lot.min…max`) — детерминированно по тайлам и seed, поэтому генератор и
   `MapRenderer` получают одну нарезку без хранения в JSON. `addHomes`: доля `homes.share`
   прямоугольных участков у переулка в жилых кварталах — жилой дом: стены в тайл, пол
   (`INTERIOR`), дверь в 2 тайла на переулок, POI `home` (x, y, w, h — пол комнаты, для мебели);
   прострел длиннее нормы — откат. Узкие переулки (2–3 тайла) не меняются. Отрисовка: у каждого
   участка своя двускатная крыша (сдержанный серый шифер с лёгкой разницей оттенков — `RENDER.tiles.roof`;
   скаты, конёк, черепица, труба, швы между домами), стены
   жилых домов — светлая штукатурка, мебель — `EffectsRenderer.drawFurniture`.
6. `ensureConnectivity`: flood fill по якорям; мелкие фрагменты засыпаются, крупные соединяются
   тоннелем минимальной стоимости (Дейкстра); недостижимые тайлы засыпаются.
7. Метрики и проверка (`validateMap`); до `GENERATOR.validation.attempts` попыток, иначе лучшая.
8. `wastes.addWastes`: карта расширяется влево, вправо и вниз — скалы (`ROCK`), в них тропа шириной
   `trail` от выхода в пустошь каждого КПП (проход сквозь стену) вдоль края и по низу карты; в
   середине нижней полосы — лагерь сопротивления (зона `rebel_camp`, POI `rebel_camp`, `camp_cache`).
   Тропа — зона `wasteland`: горожане, патрули и сканеры туда не ходят.
9. `sewers.addSewers`: карта расширяется вправо; ровно два люка — в запретной зоне у края города
   (`hatchEdge`; зона застроена — прорезается ход к площадке, `carveHatchYards`), сеть тоннелей (решётка → «растущее дерево» + петли; 3 тайла — сток
   посередине, дорожки по бокам), от каждого люка в масштабе 1:2 — ниша с лестницей и сухой ход к
   узлу, убежище с ящиками-укрытиями и тайником, рядом чёрный рынок с прилавком; засыпка мест, куда
   не пролезть. Только добавляет проходимое — связность по построению (проверяет тест).
Генерация детерминирована по seed; ~25–40 мс на попытку.

### Формат карты JSON (`world/mapIO.ts`, версия 1)
`tiles` и `zoneGrid` — массивы строк по символу на тайл (легенда в `legend`), `zones`, `pois`
(у люков — `id` пары), `stats`. Связность в F2 и тестах считается с учётом люков (`hatchLinks`). Карту можно поправить в редакторе с моноширинным шрифтом; при загрузке проверяются размеры
и символы (ошибка с номером строки/столбца), панель F2 показывает связность и прямые участки.

### ИИ NPC
- `Brain` (интерфейс) решает, что делать; каждая фракция — свой класс в `ai/brains/` с
  `StateMachine` (idle, walk, work, patrol, chase, flee, fight, arrested — по мере этапов).
- `Mover` — общее передвижение: запрос пути, следование со «сглаженным» путём, объезд встречных
  по правилу правой руки (но не в стену), распознавание блокировки:
  встречные лоб в лоб → уступает младший по `FACTIONS[*].yieldPriority` (игрок главнее всех),
  при равенстве — тот, кому ближе до «кармана»; стоящего NPC просят отойти; стоящего игрока ждут,
  потом разворачиваются; упёрся в стену — перестраивает путь.
  Сторож затора (`AI.move.giveUp*`, `ghost*`): идёт к цели, но не отходит дальше 64 px от одного места —
  через 4 с, упираясь в NPC, ненадолго проходит сквозь NPC (`Character.ghost`, физика не расталкивает;
  игрока — никогда), через 12 с бросает цель (`failed`). Тест: никто не упирается дольше 20 с.
- Цели прогулок — `ai/destinations.ts`; зоны, которых NPC избегают, — через `avoidZones` в A*.
  Сторож затора проходит сквозь и соседа вплотную сбоку (`ghostContact`), не только впереди: тот
  может прижимать к стене или завалу (глава и его охрана HYDRA на тропе).
- **Уличная жизнь** (`systems/StreetLife.ts`, `config/street.ts`, состояния `CitizenBrain`): после
  паузы житель с долей `STREET.activityChance` занят своим, а не бредёт в случайную точку —
  **chat** (подходит к свободному жителю в прямой видимости, стоят лицом друг к другу, вопрос —
  ответ из `STREET.dialogues`; потом иногда гуляют вдвоём к одной цели), **barrel** (место в круге у
  бочки с огнём — бочки во дворах-колодцах и широких местах, `EffectsRenderer.drawBarrels`),
  **home** (якорь в помещении жилого квартала, побыть там), **listen** (обращение Администратора
  на площади раз в `broadcast.every` с: строки в журнал, жители в `radius` идут слушать), **bench**
  (только при зелёном коде: место на скамейке главного проспекта, `takeBenchSeat` — где уже сидят,
  привлекательнее; вдвоём беседуют — ведёт младший по id, один — изредка бормочет; код сменился —
  встают). Фонари и скамейки (`StreetLifeSystem.lamps/benches`, `placeAvenue`, `STREET.avenue`) —
  у стен главного проспекта, детерминированно без rng: якорь 2×2 на асфальте, за ним сплошная стена;
  скамейка — два таких якоря подряд (два места), не у дверей и постов проходной. Отрисовка —
  `EffectsRenderer.drawAvenue` (пятно света — спрайт, градиент один раз). На ходу
  иногда останавливаются оглядеться (`glance`). Воры и бандиты этим не заняты; при красном коде —
  нет. Прервать может ГО, стрельба, комендантский час — собеседник тоже расходится.

### Закон (`systems/LawSystem.ts` + `ai/brains/CpBrain.ts`)
- Нарушения, заметные ГО (`observe`): бег быстрее `LAW.runSpeed`, запретная зона. Плюс плановые
  проверки CID (на КПП — почти всех в коридоре). Без CID / в розыске — выясняется при проверке.
- Цикл: `order` (стоять; NPC может побежать — шанс по фракции) → ГО подходит → `beginCheck` →
  `judge` → `apply`: отпустить / штраф (`LAW.fines`) / `arrest`. Бег от ГО = неподчинение → арест.
  Игрок, отошедший дальше `LAW.complyRadius` после приказа, — «в бегах».
- Арест: мозг задержанного сохраняется в `law.savedBrain`, ставится `PrisonerBrain` (идёт за
  конвоиром; отстал дальше `LAW.escortWait` — конвоир ждёт; конвоир и его задержанный друг друга не
  толкают — `physics`). У камеры места (`Cell.slots`: у одиночной — одно, у общей — до
  `LAW.commonCell.max` по площади). Граждан, ГСР, вортигонтов и партизан сажают сперва в **общую камеру**
  (задел под бунты), остальных — в одиночные (`freeCell(x, y, prisoner)`; мест нет — в любую). ГО ведёт
  к камере (`freeCell`/`reserve` — бронь места), `putInCell` отпирает дверь, задержанный идёт на своё
  место (`cellAnchor(cell, c)`) — `lockCells` запирает дверь, пока там кто-то сидит и никто не входит и
  не выходит (в общую заводят и отпускают, не выпуская остальных), срок `LAW.jailTime`. `occupants`,
  `slotOf`, `vacate`. После срока — выход за ворота
  Нексуса и возврат прежнего мозга. Упустил беглеца на `chaseLoseTime` — тот объявлен в розыск.
- Игрок-ГО: F — проверить того, кто перед ним → панель CheckPanel (1 отпустить, 2 штраф, 3 арест);
  арестованный идёт за игроком, у свободной камеры КПЗ заводится автоматически. Беглеца надо догнать.
- Все сообщения — через `bus.emit('log', …)` в журнал событий; реплики — `Character.say`.
- ГО сразу узнаёт повстанца (`observe` → 'rebel'): безоружного задерживает, вооружённого — бой.
  Житель с оружием в руках — нарушение 'weapon'. Красный код: на улице после `WAR.curfewGrace` —
  нарушение 'curfew' (арест). Бег в панике от стрельбы нарушением не считается (`panicUntil`).
- Отряды ГО (`config/factions.ts`, `CP_DIVISIONS`): UNION — патруль, быстрее бег; GRID — гарнизон
  КПП, MP7, G — поставить бетонный блок (до трёх); HELIX — медик, G — лечить, NPC-медики сами
  выходят к раненым; JURY — проверка CID вдвое быстрее, штрафы вдвое выше.

### Экономика (`systems/EconomySystem.ts`, `config/economy.ts`)
- Раздача рационов по таймеру (`ECONOMY.rations`): окно у будки на площади, живая очередь по
  отметкам (`queueSlot`), выдаёт работник ГСР (NPC сам, игрок-ГСР — E). Рацион = паёк + токены,
  раз за раздачу на персонажа. При комендантском часе раздача не открывается.
- Сытость падает у всех; при нуле — урон. NPC едят и перевязываются сами, если есть чем.
- Зарплаты раз в минуту: ГО (с надбавкой за ранг), ГСР — если работал, Администратор.
- Поломки (щитки у стен переулков) появляются по таймеру; ГСР чинит (игроку нужен набор инструментов).
- Магазин ГСР — комната с дверью недалеко от площади (`stampShop`), E у прилавка.
- Предметы и наборы по ролям — `config/items.ts` (`ITEMS`, `WEAPONS`, `KITS`), инвентарь — `Inventory`.

### Оружие и бой (`systems/CombatSystem.ts`, `ai/Gunner.ts`, `config/items.ts`, `config/combat.ts`)
- 9 стволов в `WEAPONS`: дубинка ГО (удар в секторе, оглушает — `speedMul`), USP, револьвер .357,
  MP7, трофейный MP7, самодельный пистолет, AR2 (пробивает укрытия), SPAS-12 (8 дробин, заряжается
  по патрону, выстрел прерывает перезарядку), арбалет. У каждого: режим огня (`semi/auto/pump/melee`),
  урон и падение урона (`effectiveRange → range`, `falloff`), конус разброса, отдача, магазин,
  перезарядка, пробитие блоков, время доставания (`draw`), замедление при прицеливании, слышимость.
- **Конус как в Foxhole**: `spreadOf(c)` — полуугол от `spreadHip` до `spreadAim` по мере
  прицеливания `c.aim` (игрок — ПКМ, NPC — пока ведёт цель; копится стоя, медленнее на ходу,
  теряется на бегу), + движение (`moveSpread`, бег — сильнее) + отдача `c.recoil` (спадает
  `recovery`/с). Пуля/дробина — треугольное распределение строго внутри конуса.
  `AimRenderer`: треугольник разброса, заливка обрезана стенами лучами DDA, дуга на предельной
  дальности (белая — прицел полный, серая с заполнением — перезарядка), штрихами — дальность полного
  урона. У игрока — всегда (от бедра бледный), у NPC — когда целится. При прицеливании камера
  отдаляется и уходит к курсору (`CAMERA.aim*`), чтобы дуга была в кадре.
- **Пешки и оружие в стиле RimWorld** (`config/pawns.ts`, `entities/PawnRenderer.ts`,
  `config/weaponSprites.ts`, `entities/WeaponRenderer.ts`, `entities/weaponPose.ts`): слои пешки —
  тень → волосы сзади/пучок → туловище-«фасолина» с затенением → одежда или бронежилет (нагрудная
  пластина с вырезом и бликом, центральный щиток, пояс с пряжкой, набедренник с подсумком) →
  наплечники поверх контура → голова с заострённым подбородком (в профиле — нос) → лицо (глаза с
  веками, рот) → причёска (короткая, лохматая с чёлкой прядями, длинная, пучок, лысый) → головной
  убор. Четыре стороны (`pawnDir`; запад — отражение востока). Внешность — от id (`lookSeed`).
  Одежда по фракциям (`PAWN.outfits`): граждане — комбинезон с воротником и молнией, ГСР — кепка
  и повязка, администратор — костюм с галстуком, повстанцы — рубашка цвета ранга, с ранга
  `vestFromRank` бронежилет и бандана. ГО и OTA (`style: 'marine'`, `marineBody`/`marineHelmet`) —
  силовая броня как у пехотинцев RimWorld: крупные наплечники, кираса с грудными пластинами,
  горжет, пояс с подсумками, набедренники, ранец; закрытый шлем без лица (тёмный визор с бликом,
  гребень, наушники, решётка подбородника). ГО — сине-серая броня, полосы на наплечниках, гребень и
  нашивка цвета ранга; OTA — светлая броня с красными полосами и красным огоньком в визоре.
  Пешка мельче круга столкновений (`PAWN.scale`), камера показывает ~820×550 px мира. Оружие — плоские модели по мотивам RimWorld
  (2–3 тона, контур), размером с пешку (`WEAPON_POSE.scale`), повёрнуты за прицелом, влево —
  отражены; на север — за спиной; перезарядка опускает ствол, отдача откатывает; трассер — от
  дула (`muzzleWorld`). Имя и роль — под ногами, реплика — над головой; тела — лежащие пешки.
  Круг столкновений (радиус 12) не менялся.
- Магазин у каждого ствола свой (`c.mags`): смена оружия (Q — следующее, H — убрать) патроны не теряет.
- ИИ (`Gunner`): ищет цели в дальности лучшего ствола, берёт `bestWeapon` под дистанцию (дубинку
  меняет на огнестрел, дробовик — только вблизи), стреляет, когда конус у цели достаточно узкий
  (`ai.fireWidth`), очередями (у полуавтомата — сериями); ГО вне боя снова берёт дубинку.
- Стены и закрытые двери останавливают пулю, бетонный блок — с шансом `barrierStopChance ×
  (1 − penetration)` один раз на блок (свой блок у стрелка не мешает).
- Урон × падение × броня фракции; раненый (< `woundedFraction`) отходит (ГО в укрытии
  перевязывается своей аптечкой); убитый NPC убирается, остаётся тело с лутом (E — обыскать).
  Игрок возрождается через `respawnDelay`, теряя часть токенов.
- `threat(a, b)`: повстанцы стреляют по Альянсу всегда, Альянс — по вооружённым и напавшим.
  Напавший на ГО/OTA получает `hostile` и розыск. Gunner не стреляет, если на линии свой.
- Звук (`ui/GunfireAudio.ts`, `config/audio.ts`): синтез WebAudio по классу оружия, тише и глуше
  с расстоянием, панорама по стороне; N — вкл/выкл. Логика звук не трогает — UI читает `combat.shots`.
- **Гранаты** (`GRENADE` в `config/combat.ts`, предмет `grenade`): `CombatSystem.throwGrenade` — летит к
  точке (стену не перелетает, блок — да), взрыв через `fuse` с от броска: урон по кругу с падением к
  краю, стена закрывает, блок ослабляет; взрыв — «выстрел» `weapon: 'grenade'` (слышат NPC, звук).
  Игрок — T к курсору. ИИ (`Gunner.tryGrenade`): цель за укрытием (скрылась после контакта или блок на
  линии) или кучка врагов, своих у точки нет, сам вне радиуса; шанс и откат. `ai/Dodge.ts`: заметил
  гранату (видит в угле обзора; бросок «своих» — слышит крик) — бежит, предпочитая место за стеной.
  Наборы: повстанцы, GRID, OTA; продаются на чёрном рынке.
- **Следы** (`combat.decals`, `COMBAT.decals`): гильзы, кровь, выбоины у стен, копоть; живут десятки
  секунд, список ограничен. Взрыв — вспышка/дым (`combat.blasts`) и тряска экрана (`Game.shake`).
- **Производительность отрисовки** (бой у КПП — десятки пешек, тел и следов): пешки и тела —
  спрайты из `PawnCache` (рисуются один раз на внешность, сторону и квантованный масштаб, дальше
  `drawImage`; `PAWN.cache`), туман — две заливки прямо на экран (без полноэкранного холста),
  следы — без `save/restore` и новых строк цвета, их не больше `COMBAT.decals.max`, цвета тайлов —
  hex. Новую отрисовку в цикле по сущностям — без `save/restore`, градиентов и сборки строк на кадр.
- **Реплики** (`systems/Barks.ts`, `config/barks.ts`): контакт, перезарядка, ранен, убил, потеря
  своего (видит тело), «вперёд» на перебежке в капте; на улице — бытовое, а если у КПП бой — о границе
  (`ai/streetBark.ts`). Откат у каждого, говорящего не перебивают.

### Война на границе (`systems/WarSystem.ts`, `ai/brains/RebelBrain.ts`, `config/war.ts`)
- Фронт = пограничный КПП из **двух отдельных дворов-точек**, как «война на D» на UnionRP: «Запад» —
  D3 → D4, «Восток» — D5 → D6 (`WAR.pointNames`), между ними шорт и лонг (`Front.short/long`,
  `shortZone/longZone`). `buildFronts` находит части по зонам: двор с воротами к пустоши — внешний,
  с воротами к городу — внутренний, из двух проходов ближе к оси ворот — шорт. `DPoint`: зона, посты,
  пол (от входа), центр. Пустошь 14 тайлов с завалами; гарнизон — часовые GRID на всех постах
  (2 во внешнем дворе, 3 во внутреннем) + медик HELIX в бункере. Проходная (`Front.gatehouse`,
  узнаётся по концу имени зоны `ZONE_NAMES.gatehouse`) — не часть точек D: там 2 RCT (роль `gate`,
  ранг 0, UNION — `Front.gatePosts`, проверяют входящих), `Front.apron` — за её дверью, в городе. `sectionAt` (0 — внешний двор,
  1 — шорт/лонг, 2 — внутренний), `pointAt`, `pointOfPost`. Разметка на полу — `drawPoints`.
- Ритм фронта «сбор → капт»: армия сопротивления приходит из лагеря тропой (этап 7) и собирается на
  пустоши вне видимости постов (`RebelBrain` режим `gather`, `pickGather`, `WAR.gatherDist`) — не
  лезет под огонь по одному. Собралось ≥ `capture.minAttackers` — все разом идут на капт очередной
  точки. Новых отрядов у КПП не появляется.
- `war.active(f)` — стреляли у КПП за `activeWindow` с: указатели на КПП у края экрана
  (`EffectsRenderer.drawFrontMarkers`, с именами точек и их состоянием), перестрелку слышно из города.
- **Капт точки** (`WAR.capture`, `startCapture`/`endCapture`/`updateCapture`): точки берутся по
  очереди (`Front.held` — сколько, с внешней, у повстанцев). Капт на `duration` с; **во время капта
  подкрепления ГО не выходят, новые отряды повстанцев не подходят**. Повстанцы в режиме `capture`
  идут перебежками по двору штурмуемой точки (`coverAt` по `DPoint.floor`): укрытие на доле пути
  `advance`, `coverWait`, `advanceStep`, под огнём — `shootStop`/`dash`. **Не одной толпой**:
  `RebelCommand.formTeams` делит штурмующих на звенья по `capture.teamSize` (глава с HYDRA — звено 0;
  подошедшие позже — в самое малое звено). У звена своя полоса двора из `lanes` (поперёк оси вход →
  центр, `lateralOf`), свой проход во внутренний двор (чётные — шорт, нечётные — лонг; без звена —
  `longChance`), точки маршрута у каждого свои; перекаты — каждые `bound` с бегут чётные или
  нечётные звенья (`boundTurn`), в проход звено входит в свой такт. Укрытие, точка сбора и точки
  маршрута — не ближе `spacing` к цели другого штурмующего (`takenSpots`, `crowdAt`). Клич NPC-главы
  двигает звенья до его рубежа своими полосами (без очереди перекатов), за игроком-главой бегут
  врассыпную (`COMMAND.rally.spread`). Внешний двор взят — сбор уже в
  нём (вне видимости постов внутреннего), а на штурм внутреннего идут и державшие внешний
  (на постах остаётся `keepOnHeld`). Итог: гарнизон точки
  (`capture.defenders` — часовые её постов, у внутренней ещё медики) перебит — или `killsToWin`
  убийств с перевесом — точка захвачена, штурмовавшие держат её посты (`hold`), лишние — назад на
  сбор; все штурмующие погибли — отбит; по таймеру — захват при ≥ `minKills` и перевесе. Захват
  точки — код жёлтый. Обе точки — **КПП прорван** (`breach`, `owner = 'rebels'`).
- **Цитадель**: погибшие ГО и OTA возвращаются у ворот Нексуса (постоянный состав) — не сразу
  (`ROSTER.respawn`: ГО 60 с, OTA 90 с, иначе гарнизон восполняется быстрее, чем повстанцы
  закрепляются) — и бегут на свои посты (`GUARD`: дальше `LAW.cpRunToPost` — бегом); часовые КПП, где идёт капт, ждут его конца.
  Захваченную точку отбивают контрудары резерва OTA (`counterattack`: через `counterDelay` с, потом
  раз в `retakeEvery` — `retakeOta` свободных OTA на посты ближней к городу захваченной точки,
  `OtaBrain.assignPost`). Точка отбита, когда в её дворе (и ближе к городу) нет повстанцев, а ГО уже
  там, `retakeCalm` с (не раньше `holdTime`). Все точки снова у Альянса — OTA в резерв.
- **Граждане к прорванному КПП** (`WAR.defect`, `updateDefection`, `ai/brains/DefectorBrain.ts`): раз в
  `every` с не-лоялист бежит к КПП (чуть медленнее «бега» — чтобы ГО не остановил; от ГО — бегом),
  в коридоре становится повстанцем (`defect`: MP7, розыск, держит пост). Не больше `max` бегущих,
  `perBreach` за прорыв, граждан не меньше `minShare` от начального числа; КПП отбит — бегущие
  возвращаются к жизни. Игрок-гражданин/ГСР в коридоре прорванного КПП — E (событие `defected`).
- **Выход в город**: только когда **все точки D** (оба КПП) у повстанцев (`cityPush`): на каждом КПП
  остаются держать посты `holdKeep`, остальные — `assault` через внутренние ворота. Боец в `assault`
  (или при `cityPush`) в городе → прорвавшийся (`infiltrators`; из отряда фронта исключён навсегда —
  `RebelCommand` не возвращает режимы `storm`/`infiltrate` в `f.squad`, иначе приказ повторялся каждый
  тик и боец стоял) → КРАСНЫЙ КОД: комендантский час, раздача закрыта,
  из Нексуса выходит OTA, патрули прочёсывают город по данным «Надзора» (`lastKnown`). Граждане
  уходят в подъезды/дворы (`shelters`). Отбой — прорвавшихся нет `calmToGreen` с (не раньше
  `redMinTime`); через `redMaxTime` прорвавшиеся «уходят в подполье». `forceAssault` — для тестов.
- **Штурм Нексуса** (`WAR.nexus`, `updateNexus`, режим `storm`) — цель выхода в город. При `cityPush`
  бойцы в `assault` сначала собираются во внутреннем дворе прорванного КПП (`staged`); собралось
  `waveSize` (или ждали `stageMax` с и собралась треть идущих, или `stageHardMax` с) — **волна** (`nexus.wave`): все разом через проходную к Нексусу,
  в городе — `storm` (перебежками к зоне Нексуса, внутри меняют позицию не кучей, раненые не
  отходят). Пока волна идёт (не дольше `waveMax`) и Нексус не пал, ГО и OTA не возрождаются
  (`RosterSystem`). В зоне Нексуса ≥ `minAttackers` штурмующих и не меньше защитников — захват
  копится `captureTime` с (иначе убывает); **Нексус захвачен** — удержать `holdToWin` с: **победа
  восстания** (`rebelVictory`: КПП снова у Альянса, все повстанцы — `withdraw` в лагерь, код
  зелёный, игрокам-повстанцам `reward`, новая цель главы); повстанцев в Нексусе не осталось — отбит.
  Табло — `CaptureBar`.
- **Терминал кодов тревоги** (POI `code_terminal`, символ `T` шаблона Нексуса — у стены кабинета
  Администратора; `ui/CodePanel`, `WarSystem.setCode`): E рядом (`WAR.terminal.reach`) — панель:
  1 жёлтый, 2 красный, 3 отбой. Доступ — Администратор и ГО с ранга `terminal.minCpRank` (OFC).
  Код с терминала (`manualCode`) держится, пока его не снимут (автоматический отбой не действует),
  но автоматическая эскалация до красного работает; погиб или разжалован включивший — снова по
  обстановке. Экран терминала — цвета кода, мигает, если код с терминала.
- Нападение «в городе» (тревога, эскалация до красного) — только если и цель, и стрелок в городе:
  огонь с постов КПП по проспекту — это фронт. `war.reinforcements = false` — тесты без контрударов.

### Канализация и сопротивление (`systems/UndergroundSystem.ts`, `systems/InsurgencySystem.ts`, `config/underground.ts`)
- Люк: E в радиусе `UNDERGROUND.useRadius` → `climbTime` с на месте → у парного люка на другом
  уровне (`climb` ставит на свободный якорь, без интерполяции). NPC ходят через люки `HatchTravel`:
  `route` выбирает пару люков по сумме «сюда → люк» + «люк → цель».
- Люков два (в запретной зоне у края), пользуются ими только партизаны (`UndergroundSystem.canUse`).
  Схрон партизан: `ROSTER.partisans` партизан (`UndergroundBrain`, постоянный состав), тайник (E:
  патроны к своим стволам, `refillAmmo`), игрок-партизан появляется здесь.
- Вылазки раз в `outingEvery` (в убежище остаётся ≥ `minAtBase`): обход тоннелей до люка, поход
  на рынок, разведка в город через люк (1–2 бойца, режим `outing`). Разведчик скрытен: огонь не
  открывает (`Gunner.holdFire`), заметил ГО — уходит вниз; отвечает, только если ранили.
- Операции раз в `opEvery`: **саботаж** (1–2 бойца через люк к узлу Альянса, `sabotageTime` с —
  узел сломан) или **засада** (2–3 бойца через люк на патрульного ГО, бой `ambushFight` с, отход).
  Раненые и закончившие — к ближайшему люку и вниз. Связь сопротивления видна только игроку-повстанцу.
- Узлы Альянса (`EconomySystem.nodes`: терминалы у стен на проспектах и площади) ломаются только
  от саботажа; чинит ГСР (дороже щитка). Игрок-повстанец саботирует сам (E), награда `sabotageReward`.
- **Тревога Администратора — код жёлтый** (`WarSystem.raiseAlarm`): саботаж или ранение сотрудника
  Альянса в городе (`CombatSystem.onDamage`). Нападавшие → `war.operatives` (ушли под землю — след
  потерян), точка тревоги держится `ALARM.pointTime`. Патрули в `respondRadius` → «hunt»: не толпой в
  последнюю известную позицию / точку тревоги, а прочёсывание вокруг неё (`CpBrain.sweep`, `LAW.hunt`:
  у каждого своя точка не ближе `spacing` к точкам других, осмотрелся — следующая); проверки CID чаще (`LAW.alarmCheckMul`). Отбой — `calmToGreen`
  без известных нападавших (не раньше `minTime`); погибло `escalateKills` сотрудников — красный код.
- Чёрный рынок (прилавок в канализации, торговец — `PostBrain`): оружие и патроны
  (`ECONOMY.blackMarket.stock`), скупка трофеев и рационов (`sell`), поддельная CID (снимает розыск).
- ИИ без «глаз на спине»: `Gunner.acquire` — только в угле обзора `VISION.npcFovDeg` или вплотную;
  ранили / выстрел врага рядом → через реакцию поворот в примерную сторону (`COMBAT.ai.alert*`);
  мозги зовут `gunner.look` последним, чтобы бой и тревога перебивали дежурный взгляд.

### Этап 5: лояльность, чат, карты, сохранение (`config/loyalty.ts`, `config/chat.ts`, `config/minimap.ts`, `config/save.ts`)
- **Лояльность** (`Character.loyalty`, только граждане и ГСР — `hasLoyalty`): + за полученный рацион,
  пройденную проверку, работу ГСР, донос, поощрение ГО; − за штраф, арест, оскорбление, ложный донос.
  Уровни (`LOYALTY.tiers`): неблагонадёжный → гражданин → лоялист → образцовый: множитель плановых
  проверок CID (`checkMul`), надбавка к рациону, скидка в магазине ГСР (`EconomySystem.shopPrice`).
  Граждане-лоялисты (от `LOYALTY.uniform.min`) — в светло-фиолетовой форме и с такой же подписью
  (`isLoyalistUniform` в `EntityRenderer`).
- **Чат** (`ChatSystem.submit`, ввод — `ui/ChatBox`, Enter или «/»): речь — реплика и строка журнала
  (вид `chat`); ГО, который видит, на оскорбление (`CHAT.insult`) требует документы — нарушение
  `insult`, штраф; на приветствие отвечает прохожий. Команды: /me, /r (рация ГО), /roll, /cid,
  /донос (виден повстанец/вооружённый/в розыске: повстанец — тревога, иначе патрульный идёт
  разбираться), /поощрить (ГО), /помощь.
- **Карты** (`ui/MapView`): мини-карта в углу и большая карта (M) уровня игрока, собираются из тайлов
  один раз; канализация открывается по мере исследования (повстанцам — сразу), люки — найденные.
  Маркеры: КПП (бой/капт/захвачен), Нексус, раздача, магазин, сломанные узлы, тревога, свои.
- **Сохранение** (`SaveGame`, localStorage `SAVE.key`): роль, имя, деньги, лояльность, здоровье,
  сытость, инвентарь и оружие, статус CID, позиция, знание карты; seed карты. Автосохранение раз в
  `SAVE.interval` с, при выборе роли и закрытии вкладки. Без ?seed (или с тем же seed) игра
  продолжается; «Новая игра» (в главном меню — экран подтверждения, в меню роли — второе нажатие; `window.confirm` в песочнице не работает) стирает сохранение. Задержанный сохраняется в розыске.
- **Пауза** — P. **Меню** (`ui/GameMenu`): главное при запуске (продолжить / играть, новая игра,
  управление, звук) и пауза по Esc (продолжить, сохранить, сменить роль, управление, звук, выйти в
  главное меню — с сохранением). Пока меню открыто, мир стоит. Esc сначала закрывает открытые панели
  (`Game.onEscape`, перехват до остальных обработчиков); список клавиш — `CONTROLS_HELP` в `HelpBar`.

### Этап 6: профессии (`config/professions.ts`, `config/labor.ts`, `config/crime.ts`, `config/cpUnits.ts`)
- `Character.profession` (`ProfessionId`), `PROFESSIONS` — описание, умения (для `RoleMenu`), набор;
  `DEFAULT_PROFESSION`, `professionsOf(faction)`. Сохраняется в `SaveGame` (`role.profession`).
  Одежда по профессии — `PAWN.professionOutfits`. Новая фракция `vort` (вортигонты: без лояльности,
  ГО их не проверяет, `LawSystem.checkable`).
- **Цепочка снабжения ГСР** (`LaborSystem`): фасовщик собирает коробки на заводе в промзоне
  (`packStep`), курьер носит их к будке (`takeBox`/`deliverBox`, `Character.carrying`), склад будки
  `EconomySystem.rationStock` расходуется поваром на раздаче (пусто — `onEmpty`, рацион не выдаётся).
  Уборщик и вортигонт убирают мусор (`trash`, `cleanStep`), отброс — роется (`search`), медик ГСР
  лечит за плату (`treat`). NPC берут работу в `CitizenBrain.pickJob` по профессии.
- **Бумажная работа лоялистов** (`LABOR.paperwork`, `LaborSystem.desks/claimDesk/payPaperwork`, работа
  `paper` в `CitizenBrain`): граждане с лояльностью ≥ `minLoyalty` (доля `AI.population.loyalistShare`)
  ходят в канцелярию Нексуса (Нексус им открыт на время работы), сидят за свободным столом и раз в
  `payEvery` с получают `pay` токенов и +лояльность; во время раздачи сперва идут в очередь.
  Игрок-лоялист — E у стола (`workTime` с).
- **Преступность** (`CrimeSystem`): карманная кража только со спины (`behind`), свидетель-ГО —
  `law.crimeUntil` → нарушение `theft` (и причина ареста в `judge`); жертва может заметить и крикнуть;
  взлом раздатчика отмычкой. NPC-воры (`CRIME.npc`) выходят на кражу, когда ГО не видно.
- **Лояльность**: у уровней `perks` (`run` — бег без нарушения, `queue` — очередь впереди, `escort` —
  `/охрана`, BODYGUARD у `CpBrain`).
- **ГО**: ранги RCT…DVL, отряды MPF/GRID/MEDIC/OBS(`jury`)/TECH. OBS: состояние SCAN — осмотр тела,
  `CrimeSystem.investigate` объявляет убийцу (`Corpse.killer`) в розыск. TECH: дрон `ScannerSystem`
  (время жизни и откат, облёт, повстанец/вооружённый → `raiseAlarm`, розыск → ближайший патрульный).
- **Повстанцы**: в отрядах WarSystem медики (`RebelBrain.medic`) и пиро; партизан — `disguised`
  (`isHostile`/`observe` его не узнают; оружие в руках или проверка — `reveal`).
- **Огонь** (`FIRE`): граната пиро оставляет зону (`combat.fires`), болт поджигает; `ignite` →
  `burnUntil`, урон `dps`. **Крематор** (`CrematorBrain`, `LaborSystem.ensureCremator`) — один на
  город, выходит из Нексуса, сжигает тела (`Corpse.burning`).
- Тесты: `tests/professions.test.ts`, `tests/units.test.ts`.

### Этап 7: постоянный состав, армия сопротивления, выборы (`config/roster.ts`, `config/election.ts`)
- **Постоянный состав** (`systems/Roster.ts`): у каждого NPC роль `Character.role` (`RoleSpec`: вид,
  фракция, профессия, отряд, набор, у часовых и RCT проходной — фронт и пост). Всех создаёт `spawnRole` (заселение
  — `Population.spawnPopulation`), погибший через `ROSTER.respawn[вид]` с появляется снова на
  спавне своей стороны (`respawnPoint`: ГО — казарма Нексуса (`bunk`), OTA — комната OTA (`ota_spot`), армия — лагерь, партизаны —
  схрон, жители — жилые кварталы) с тем же именем и делом. Новых персонажей не создаётся (кроме
  крематора `LaborSystem`). Гибель слушают через `CombatSystem.deathListeners`.
- **Армия сопротивления** (`ROSTER.army`, `ROSTER.hydra`): глава восстания (250 HP, красный берет),
  4 ветерана (AR2), 6 солдат, пиротехник, подрывник (гранаты чаще — `GRENADE.ai.demoMul`) и спецотряд
  HYDRA (капитан, 2 офицера, 3 бойца — как SAS: балаклава, противогаз, бронежилет; держатся рядом с
  главой, идут его маршрутом). `systems/RebelCommand.ts`: глава выбирает КПП (`pickTarget`: где точки
  уже наши, иначе меньше защитников; после `retargetAfterFails` неудачных каптов — пересмотр),
  большинство идёт туда, `COMMAND.diversion` бойцов — на второй КПП (режим `raid`). КПП прорван —
  его посты держат захватившие, а глава ведёт армию на следующий непрорванный (`breach` →
  `pickTarget`); захваченную точку нельзя отбить раньше `capture.holdTime` (200 с) — армия успевает
  дойти по тропе до второго КПП, и при всех точках D повстанцы выходят в город. Игрок-глава —
  армия идёт на КПП, у которого он; выбрал главу игрок — NPC-глава становится ветераном
  (`demoteNpcLeader`), главы нет — им становится ветеран (`ensureLeader`). По тропе армия идёт
  быстрым шагом (`trailSpeed`). Раненые и без патронов — в лагерь (`retreat` → `camp`: лечение
  `campHeal`, патроны), готовые снова выходят. **Клич** (`shout`, у игрока — G): бойцы в
  `rally.radius` `rally.time` с бегут за главой и вместе идут на штурм (`rallyFor`); NPC-глава
  кричит в капте, как только клич готов. HUD игрока-главы показывает откат клича.
- **Резерв OTA** (`ROSTER.ota`: элита — белая броня, красный глаз; солдаты — серо-синие, голубой
  глаз): `OtaBrain` — reserve в комнате OTA у своего шкафа, post (контрудар), hunt (красный код), home.
- **Выборы Администратора** (`systems/ElectionSystem.ts`): Администратор погиб — выборы на
  `ELECTION.duration` с среди `candidates` самых лояльных (не ниже `minLoyalty`); NPC голосуют сами
  (чаще за лояльного), игрок — `/голос N`, ход — `/выборы`. Победитель становится Администратором
  и идёт в кабинет; игрок-победитель — событие `elected`. Администратор не возрождается.
- **Новые профессии** (UnionRP): бандит (гоп-стоп лицом к лицу в подворотне со стволом — `CRIME.rob`,
  `CrimeSystem.rob`, у игрока — E), беглец (без CID, в розыске, бежит к прорванному КПП первым),
  глава, ветеран, подрывник, HYDRA, солдат и элита OTA.
- Тесты: `tests/stage7.test.ts`.

## Соглашения
- Все числа, цвета, скорости, зарплаты и т.п. — в `src/config/*`. Новый параметр → в конфиг.
- `src/` не использует API Node; `tests/` может (свой `tests/tsconfig.json`).
- Случайность в генерации и мире — только через `Rng` (seed), не `Math.random` (кроме выбора нового seed).
- Логика не трогает DOM; UI подписывается на `EventBus`. Новые события — в `GameEvents`.
- Персонаж игрока и NPC — один класс `Character`; различие только в источнике управления.
- Отрисовка тайлов — в экранных пикселях с округлением (без щелей); подписи — в экранных пикселях.
- Перед коммитом: `npm run typecheck && npm test`.

## Этапы
- [x] **Этап 1 (MVP)**: игровой цикл, генератор переулков с коллизиями и проверкой связности,
  движение игрока, камера, 20 NPC-граждан бродят по A* (уступают в узких проходах), HUD
  (здоровье, токены), названия зон, панель карты (перегенерация, JSON), отладка ИИ.
- [x] **Этап 2**: туман войны (лучи), двери (открываются, перекрывают обзор, камеры запираются),
  фракции с рангами (цвет по рангу), выбор роли (при старте и у терминала найма), патрули и посты ГО,
  проверка CID, штраф/арест/погоня/розыск, конвой и КПЗ со сроком, два пограничных КПП с коридором
  для «коридорной рубки» и часовыми, журнал событий и реплики.
- [x] **Этап 3**: экономика (рационы по таймеру с очередью, голод, зарплаты), работа ГСР (выдача,
  ремонт, прилавок), магазин, инвентарь. По просьбе пользователя сюда же перенесены из этапа 4:
  бой, постоянная перестрелка у пограничных КПП с отрядами повстанцев, прорывы → красный код,
  комендантский час, OTA; отряды ГО (UNION/GRID/HELIX/JURY); реакция ГО на повстанцев.
- [x] **Этап 4**: канализация (отдельная область той же сетки + люки-порталы) как база повстанцев,
  саботаж и нападения повстанцев в городе, тревога Администратора (код жёлтый) при нападениях
  внутри города, чёрный рынок. Плюс: оружие детальнее с конусом прицела как в Foxhole, звук
  выстрелов, автоматы у повстанцев, NPC без «глаз на спине».
- [x] **Этап 5**: лояльность, чат и команды, мини-карта и большая карта (M), сохранение в
  localStorage, пауза; капт КПП. После: гранаты, реплики, следы боя, сбор повстанцев перед каптом
  без подкреплений ГО, пешки и оружие в стиле RimWorld.
- [x] **Этап 6**: профессии по мотивам UnionRP: цепочка снабжения ГСР, воры и отбросы, вортигонты,
  лоялисты с привилегиями, ранги и отряды ГО (OBS, TECH-сканеры), медики/пиро/партизаны, крематор.
  После: «война на D» — КПП из двух дворов D3–D4 / D5–D6 с шортом и лонгом, подкрепления из Цитадели, граждане
  уходят к прорванному КПП, выход повстанцев в город только при всех точках D; ГО — большинство.
- [x] **Карта-городок** (после этапа 7): застройка — отдельные дома с крышами, жилые дома с комнатой и
  дверью (вместо подъездов-коридоров), узкие переулки сохранены; Нексус 36×30 — 7 камер, казарма ГО,
  комната OTA, канцелярия для лоялистов, кабинет с терминалом кодов. После: коридоры Нексуса в 4
  тайла, общая камера для граждан и партизан, лоялисты в светло-фиолетовом, фонари и скамейки на
  главном проспекте (сидят и беседуют при зелёном коде), приглушённые крыши. Дальше — перепланировка
  проспектов и улиц.
- [x] **Этап 7**: пустошь вокруг города с лагерем сопротивления и прямой тропой к КПП; два люка в
  запретной зоне только для партизан; постоянный состав с возрождением; армия: глава с кличем,
  ветераны, солдаты, пиро, подрывник, HYDRA (SAS); глава выбирает КПП; резерв OTA; выборы
  Администратора; бандиты, беглецы.

Работа строго поэтапно: после каждого этапа — отчёт и ожидание подтверждения пользователя.
